<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="using:Mnemo.UI.Controls"
             x:Class="Mnemo.UI.Controls.TagChipInput"
             x:Name="root"
             Focusable="False">
    <UserControl.Styles>
        <Style Selector="Border.tag-chip">
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="8,4,4,4" />
            <Setter Property="Background" Value="{DynamicResource WidgetBackgroundPrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
        <Style Selector="Border.tag-chip TextBlock">
            <Setter Property="FontSize" Value="{DynamicResource FontSize.Body.Small}" />
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style Selector="Button.tag-chip-remove">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="4" />
            <Setter Property="CornerRadius" Value="6" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style Selector="Button.tag-chip-remove:pointerover /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="{DynamicResource WidgetBackgroundHoverBrush}" />
            <Setter Property="CornerRadius" Value="6" />
        </Style>
        <Style Selector="Border.tag-input-host">
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="{DynamicResource WorkspaceBackgroundBrush}" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="MinHeight" Value="40" />
        </Style>
        <Style Selector="Border.tag-input-host:focus-within">
            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
        </Style>
    </UserControl.Styles>

    <Border Classes="tag-input-host">
        <StackPanel Spacing="6">
            <ItemsControl x:Name="PART_ChipList">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Classes="tag-chip" Margin="0,0,6,6">
                            <StackPanel Orientation="Horizontal" Spacing="4" VerticalAlignment="Center">
                                <TextBlock Text="{Binding}" />
                                <Button Classes="tag-chip-remove"
                                        Click="OnRemoveTagClick"
                                        Tag="{Binding}"
                                        ToolTip.Tip="Remove tag">
                                    <controls:SvgIcon SvgPath="avares://Mnemo.UI/Icons/Common/x.svg"
                                                      Width="12" Height="12"
                                                      Color="{DynamicResource TextSecondaryBrush}" />
                                </Button>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <TextBox x:Name="PART_Entry"
                     Background="Transparent"
                     BorderThickness="0"
                     Padding="4,2"
                     MinHeight="28"
                     FontSize="{DynamicResource FontSize.Body.Small}"
                     PlaceholderText="{Binding Placeholder, ElementName=root}"
                     KeyDown="OnEntryKeyDown"
                     LostFocus="OnEntryLostFocus" />
        </StackPanel>
    </Border>
</UserControl>
