namespace Mnemo.UI.Modules.Mindmap.ViewModels;

/// <summary>A selectable layout algorithm in the editor's layout switcher: the registry id and its label.</summary>
public sealed record MindmapLayoutOption(string Id, string Label);
