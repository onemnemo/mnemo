<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:Mnemo.UI.Modules.Overview.Widgets.RecentDecks"
             xmlns:controls="using:Mnemo.UI.Controls"
             mc:Ignorable="d" d:DesignWidth="500" d:DesignHeight="240"
             x:Class="Mnemo.UI.Modules.Overview.Widgets.RecentDecks.RecentDecksWidgetView"
             x:Name="Root"
             x:DataType="vm:RecentDecksWidgetViewModel"
             HorizontalAlignment="Stretch">

    <Grid RowDefinitions="*" HorizontalAlignment="Stretch" MinWidth="0">
        <!-- Header removed to avoid duplication with WidgetContainer header -->
        
        <!-- Recent Decks List -->
        <ScrollViewer Grid.Row="0"
                      HorizontalAlignment="Stretch"
                      HorizontalContentAlignment="Stretch"
                      VerticalContentAlignment="Top"
                      VerticalScrollBarVisibility="Hidden"
                      HorizontalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding RecentDecks}"
                          HorizontalAlignment="Stretch"
                          Classes="overview-recent-list">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" Spacing="0"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.Styles>
                    <Style Selector="ItemsControl.overview-recent-list /template/ ItemsPresenter > StackPanel > ContentPresenter">
                        <Setter Property="HorizontalAlignment" Value="Stretch"/>
                    </Style>
                </ItemsControl.Styles>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="vm:RecentDeckItem">
                        <Button Background="Transparent"
                                BorderThickness="0"
                                Padding="{DynamicResource ListRowPadding}"
                                Margin="0,0,0,8"
                                CornerRadius="{DynamicResource Radius.Md}"
                                HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Stretch"
                                MinWidth="0"
                                Cursor="Hand"
                                Command="{Binding #Root.DataContext.OpenDeckCommand}"
                                CommandParameter="{Binding DeckId}">
                            <Button.Styles>
                                <Style Selector="Button:pointerover /template/ ContentPresenter">
                                    <Setter Property="Background" Value="{DynamicResource ListItemHoverBackgroundBrush}"/>
                                </Style>
                            </Button.Styles>

                            <Grid ColumnDefinitions="Auto,*,Auto" HorizontalAlignment="Stretch" MinWidth="0">
                                <!-- Icon: tinted tile with accent glyph (list-row icon pattern) -->
                                <Border Grid.Column="0"
                                        Background="{DynamicResource CardIconBackgroundBrush}"
                                        Width="36"
                                        Height="36"
                                        CornerRadius="{DynamicResource Radius.Md}"
                                        Margin="0,0,12,0">
                                    <controls:SvgIcon SvgPath="avares://Mnemo.UI/Icons/Common/book.svg"
                                                      Width="18" Height="18"
                                                      Color="{DynamicResource CardAccentBrush}"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                </Border>

                                <!-- Deck Info -->
                                <StackPanel Grid.Column="1"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Stretch"
                                            MinWidth="0"
                                            Spacing="0">
                                    <TextBlock Text="{Binding Name}"
                                               FontSize="{DynamicResource FontSize.Body.Small}"
                                               FontFamily="{DynamicResource Font.SemiBold}"
                                               Foreground="{DynamicResource TextPrimaryBrush}"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding MetaText}"
                                               FontSize="{DynamicResource FontSize.Caption}"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               Margin="0,2,0,0"
                                               TextTrimming="CharacterEllipsis"/>
                                </StackPanel>

                                <!-- Last Practiced -->
                                <TextBlock Grid.Column="2"
                                           Text="{Binding LastPracticedText}"
                                           FontSize="{DynamicResource FontSize.Caption}"
                                           Foreground="{DynamicResource TextTertiaryBrush}"
                                           VerticalAlignment="Center"
                                           Margin="12,0,0,0"
                                           TextTrimming="CharacterEllipsis"/>
                            </Grid>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
