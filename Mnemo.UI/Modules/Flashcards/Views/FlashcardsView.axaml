<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Mnemo.UI.Modules.Flashcards.ViewModels"
             xmlns:components="using:Mnemo.UI.Components"
             xmlns:controls="using:Mnemo.UI.Controls"
             xmlns:markup="using:Mnemo.UI.Markup"
             xmlns:models="using:Mnemo.Core.Models.Flashcards"
             xmlns:flashcardsVm="using:Mnemo.UI.Modules.Flashcards.ViewModels"
             xmlns:views="using:Mnemo.UI.Modules.Flashcards.Views"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="1100" d:DesignHeight="700"
             x:Class="Mnemo.UI.Modules.Flashcards.Views.FlashcardsView"
             x:DataType="vm:FlashcardsViewModel"
             Foreground="{DynamicResource TextPrimaryBrush}"
             x:Name="root"
             ClipToBounds="False">

    <UserControl.Styles>
        <Style Selector="Border.library-pane">
            <Setter Property="Background" Value="{DynamicResource NotesPaneBackgroundBrush}" />
        </Style>

        <Style Selector="Button.library-nav">
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Padding" Value="12,10" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
        </Style>
        <Style Selector="Button.library-nav:pointerover">
            <Setter Property="Background" Value="{DynamicResource WidgetBackgroundPrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
        </Style>
        <Style Selector="Button.library-nav:pressed">
            <Setter Property="Background" Value="{DynamicResource WidgetBackgroundPrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
        </Style>

        <Style Selector="TextBlock.library-section">
            <Setter Property="FontFamily" Value="{DynamicResource Font.SemiBold}" />
            <Setter Property="Foreground" Value="{DynamicResource NotesSectionHeaderForegroundBrush}" />
            <Setter Property="FontSize" Value="{DynamicResource FontSize.Body.ExtraSmall}" />
            <Setter Property="Margin" Value="12,10,0,8" />
        </Style>

        <Style Selector="Border.library-divider">
            <Setter Property="Height" Value="1" />
            <Setter Property="Background" Value="{DynamicResource NotesPaneDividerBrush}" />
            <Setter Property="Margin" Value="12,10" />
            <Setter Property="Opacity" Value="0.5" />
        </Style>

        <!-- Deck cards: Border only (no Button / ContentPresenter theme = no square grey plate) -->
        <Style Selector="Border.deck-card">
            <Setter Property="Width" Value="280" />
            <Setter Property="MinHeight" Value="168" />
            <Setter Property="Margin" Value="0,0,20,20" />
            <Setter Property="Background" Value="{DynamicResource CardBackgroundSecondaryBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="{DynamicResource Radius.Xl}" />
            <Setter Property="Padding" Value="18,16" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BoxShadow" Value="0 6 18 -10 #3A000000" />
            <Setter Property="Transitions">
                <Transitions>
                    <BoxShadowsTransition Property="BoxShadow" Duration="0:0:0.2" Easing="CubicEaseOut" />
                </Transitions>
            </Setter>
        </Style>
        <Style Selector="Border.deck-card:pointerover">
            <Setter Property="BorderBrush" Value="{DynamicResource CardAccentBrush}" />
            <Setter Property="BoxShadow" Value="0 18 36 -8 #46000000" />
        </Style>
        <Style Selector="Border.deck-card:pressed">
            <Setter Property="BoxShadow" Value="0 10 22 -8 #3A000000" />
        </Style>

        <Style Selector="ItemsControl.deck-library-grid WrapPanel > ContentPresenter">
            <Setter Property="Background" Value="Transparent" />
        </Style>

        <Style Selector="Border.deck-due-pill">
            <Setter Property="Background" Value="{DynamicResource WidgetBackgroundPrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource CardAccentBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Padding" Value="8,4" />
        </Style>
    </UserControl.Styles>

    <Panel>
        <SplitView DisplayMode="Inline"
                   IsPaneOpen="{Binding IsSidebarOpen}"
                   OpenPaneLength="280"
                   CompactPaneLength="0"
                   Background="Transparent"
                   PaneBackground="{DynamicResource NotesPaneBackgroundBrush}">
            <SplitView.Pane>
                <!-- Library sidebar -->
                <Border Classes="library-pane">
                    <Grid ColumnDefinitions="*,Auto">
                        <ScrollViewer Grid.Column="0"
                                      Classes="sidebar-scroll"
                                      Margin="0,0,12,0"
                                      VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled">
                        <StackPanel Margin="16,20,16,16" Spacing="4">
                        <Grid ColumnDefinitions="*,Auto" Margin="0,0,0,2">
                            <TextBlock Grid.Column="0"
                                       Classes="library-section"
                                       Text="{markup:T Ns=Flashcards, Key=Title}" />
                            <Button Grid.Column="1"
                                    Command="{Binding ToggleSidebarCommand}"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Padding="6"
                                    CornerRadius="8"
                                    VerticalAlignment="Center"
                                    ToolTip.Tip="{markup:T Ns=Flashcards, Key=ToggleSidebar}">
                                <controls:SvgIcon SvgPath="avares://Mnemo.UI/Icons/Common/layout-sidebar.svg"
                                                  Width="18"
                                                  Height="18"
                                                  Color="{DynamicResource TextSecondaryBrush}" />
                            </Button>
                        </Grid>

                        <Button Classes="ghost library-nav"
                                Command="{Binding SelectAllDecksCommand}">
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <controls:SvgIcon SvgPath="avares://Mnemo.UI/Icons/Common/book.svg"
                                                  Width="18" Height="18" Color="{DynamicResource TextSecondaryBrush}" />
                                <TextBlock Text="{markup:T Ns=Flashcards, Key=AllDecks}" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>

                        <Button Classes="ghost library-nav"
                                Command="{Binding SelectDueDecksCommand}">
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <controls:SvgIcon SvgPath="avares://Mnemo.UI/Icons/Common/bell.svg"
                                                  Width="18" Height="18" Color="{DynamicResource TextSecondaryBrush}" />
                                <TextBlock Text="{markup:T Ns=Flashcards, Key=DueToday}" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>

                        <Border Classes="library-divider" />

                        <Grid ColumnDefinitions="*,Auto" Margin="12,4,8,4">
                            <TextBlock Grid.Column="0"
                                       Text="{markup:T Ns=Flashcards, Key=Folders}"
                                       FontFamily="{DynamicResource Font.SemiBold}"
                                       FontSize="{DynamicResource FontSize.Body.ExtraSmall}"
                                       Foreground="{DynamicResource NotesSectionHeaderForegroundBrush}"
                                       VerticalAlignment="Center" />
                            <Button Grid.Column="1"
                                    Command="{Binding CreateFolderCommand}"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Padding="4"
                                    ToolTip.Tip="{markup:T Ns=Flashcards, Key=NewFolder}">
                                <controls:SvgIcon SvgPath="avares://Mnemo.UI/Icons/Common/folder-plus-corner.svg"
                                                  Width="16"
                                                  Height="16"
                                                  Color="{DynamicResource TextSecondaryBrush}" />
                            </Button>
                        </Grid>

                        <ItemsControl ItemsSource="{Binding FlatFolderItems}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="flashcardsVm:FlashcardFolderItemViewModel">
                                    <views:FlashcardFolderRow />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        </StackPanel>
                        </ScrollViewer>
                        <Border Grid.Column="1"
                                Width="1"
                                Background="{DynamicResource NotesPaneDividerBrush}" />
                    </Grid>
                </Border>
            </SplitView.Pane>

            <ScrollViewer HorizontalScrollBarVisibility="Disabled" ClipToBounds="False">
                <StackPanel Margin="36,28,36,32" Spacing="28" ClipToBounds="False">

                <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="10">
                    <Button Grid.Column="0"
                            Command="{Binding ToggleSidebarCommand}"
                            IsVisible="{Binding !IsSidebarOpen}"
                            Background="Transparent"
                            BorderThickness="0"
                            Padding="6"
                            CornerRadius="8"
                            VerticalAlignment="Top"
                            ToolTip.Tip="{markup:T Ns=Flashcards, Key=ToggleSidebar}">
                        <controls:SvgIcon SvgPath="avares://Mnemo.UI/Icons/Common/layout-sidebar.svg"
                                          Width="20"
                                          Height="20"
                                          Color="{DynamicResource TextSecondaryBrush}" />
                    </Button>

                    <StackPanel Grid.Column="1" Spacing="4" VerticalAlignment="Center">
                        <TextBlock Text="{markup:T Ns=Flashcards, Key=Title}"
                                   FontSize="{DynamicResource FontSize.Heading1}"
                                   FontFamily="{DynamicResource Font.Bold}" />
                        <TextBlock Text="{markup:T Ns=Flashcards, Key=Subtitle}"
                                   FontSize="{DynamicResource FontSize.Body.Medium}"
                                   Foreground="{DynamicResource TextSecondaryBrush}" />
                    </StackPanel>
                    <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                        <Button Classes="ghost"
                                Padding="10,8"
                                Click="OnTransferClick"
                                Tag="transfer">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <controls:SvgIcon SvgPath="avares://Mnemo.UI/Icons/Flyout/export.svg"
                                                  Width="16" Height="16" />
                                <TextBlock Text="{markup:T Ns=Flashcards, Key=Transfer}" />
                            </StackPanel>
                        </Button>
                        <Button VerticalAlignment="Center"
                                Classes="accent"
                                Command="{Binding CreateDeckCommand}"
                                BorderThickness="0"
                                Padding="16,10" CornerRadius="10">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <controls:SvgIcon SvgPath="avares://Mnemo.UI/Icons/Common/plus.svg"
                                                  Width="18" Height="18"
                                                  Color="{DynamicResource AccentButtonForegroundBrush}" />
                                <TextBlock Text="{markup:T Ns=Flashcards, Key=NewDeck}"
                                           FontFamily="{DynamicResource Font.SemiBold}"
                                           Foreground="{DynamicResource AccentButtonForegroundBrush}" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>

                <TextBox Classes="page-search"
                         Width="420"
                         HorizontalAlignment="Left"
                         Text="{Binding SearchText}"
                         PlaceholderText="{markup:T Ns=Flashcards, Key=SearchPlaceholder}">
                    <TextBox.InnerLeftContent>
                        <controls:SvgIcon SvgPath="avares://Mnemo.UI/Icons/Common/search.svg"
                                          Width="16" Height="16"
                                          Margin="12,0,0,0"
                                          Color="{DynamicResource TextSecondaryBrush}"/>
                    </TextBox.InnerLeftContent>
                </TextBox>

                <!-- First-run: no decks exist anywhere -->
                <components:EmptyState IsVisible="{Binding ShowEmptyState}"
                                       IconPath="avares://Mnemo.UI/Icons/Common/book.svg"
                                       Title="{markup:T Ns=Flashcards, Key=LibraryEmptyTitle}"
                                       Description="{markup:T Ns=Flashcards, Key=LibraryEmptyDescription}"
                                       ActionText="{markup:T Ns=Flashcards, Key=NewDeck}"
                                       ActionCommand="{Binding CreateDeckCommand}"
                                       Margin="0,48,0,0"/>

                <!-- Decks exist but the folder/search filter matches none -->
                <components:EmptyState IsVisible="{Binding ShowNoResultsState}"
                                       IconPath="avares://Mnemo.UI/Icons/Common/search.svg"
                                       Title="{markup:T Ns=Flashcards, Key=NoResultsTitle}"
                                       Description="{markup:T Ns=Flashcards, Key=NoResultsDescription}"
                                       Margin="0,48,0,0"/>

                <ItemsControl Classes="deck-library-grid" ItemsSource="{Binding FilteredDecks}" ClipToBounds="False">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" ClipToBounds="False" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="vm:FlashcardDeckRowViewModel">
                            <Border Classes="deck-card"
                                    PointerPressed="OnDeckCardPointerPressed"
                                    PointerMoved="OnDeckCardPointerMoved"
                                    PointerReleased="OnDeckCardPointerReleased">
                                <Grid RowDefinitions="Auto,*,Auto">
                                    <Grid Grid.Row="0" ColumnDefinitions="*,Auto,Auto" ColumnSpacing="8">
                                        <Grid Grid.Column="0" ColumnDefinitions="Auto,*" ColumnSpacing="8">
                                            <Border Width="24" Height="24"
                                                    Background="{DynamicResource CardIconBackgroundBrush}"
                                                    CornerRadius="6"
                                                    VerticalAlignment="Top">
                                                <controls:SvgIcon SvgPath="avares://Mnemo.UI/Icons/Common/file-text.svg"
                                                                  Width="14" Height="14"
                                                                  HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center"
                                                                  Color="{DynamicResource CardAccentBrush}" />
                                            </Border>
                                            <TextBlock Grid.Column="1"
                                                       Text="{Binding Name}" TextWrapping="Wrap"
                                                       FontFamily="{DynamicResource Font.SemiBold}"
                                                       FontSize="{DynamicResource FontSize.Heading5}"
                                                       MaxLines="2" TextTrimming="CharacterEllipsis"
                                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                                       VerticalAlignment="Top" Margin="0,0,8,0" />
                                        </Grid>
                                        <Button Grid.Column="1"
                                                Background="Transparent"
                                                BorderThickness="0"
                                                Padding="0"
                                                Command="{Binding #root.StartQuickSessionCommandProxy}"
                                                CommandParameter="{Binding}"
                                                IsVisible="{Binding ShowDueBadge}"
                                                ToolTip.Tip="{markup:T Ns=Flashcards, Key=SessionQuickStart}">
                                            <Border Classes="deck-due-pill"
                                                    VerticalAlignment="Top">
                                                <StackPanel Orientation="Horizontal" Spacing="4">
                                                    <controls:SvgIcon SvgPath="avares://Mnemo.UI/Icons/Common/player-play.svg"
                                                                      Width="12" Height="12" Color="{DynamicResource CardAccentBrush}" />
                                                    <TextBlock Text="{Binding DueBadgeText}"
                                                               FontSize="{DynamicResource FontSize.Caption}"
                                                               FontFamily="{DynamicResource Font.SemiBold}"
                                                               Foreground="{DynamicResource CardAccentBrush}"
                                                               VerticalAlignment="Center" />
                                                </StackPanel>
                                            </Border>
                                        </Button>
                                        <Button Grid.Column="2"
                                                Background="Transparent"
                                                BorderThickness="0"
                                                Padding="4"
                                                CornerRadius="8">
                                            <controls:SvgIcon SvgPath="avares://Mnemo.UI/Icons/Common/dots-vertical.svg"
                                                              Width="16" Height="16"
                                                              Color="{DynamicResource TextSecondaryBrush}" />
                                            <Button.Flyout>
                                                <MenuFlyout Placement="BottomEdgeAlignedRight">
                                                    <MenuItem Header="{markup:T Ns=Flashcards, Key=OpenDeck}"
                                                              Command="{Binding #root.OpenDeckCommandProxy}"
                                                              CommandParameter="{Binding}">
                                                        <MenuItem.Icon>
                                                            <controls:SvgIcon SvgPath="avares://Mnemo.UI/Icons/Flyout/open.svg"
                                                                              Width="16" Height="16" />
                                                        </MenuItem.Icon>
                                                    </MenuItem>
                                                    <MenuItem Header="{markup:T Ns=Flashcards, Key=Practice}">
                                                        <MenuItem.Icon>
                                                            <controls:SvgIcon SvgPath="avares://Mnemo.UI/Icons/Flyout/practice.svg"
                                                                              Width="16" Height="16" />
                                                        </MenuItem.Icon>
                                                        <MenuItem Header="{markup:T Ns=Flashcards, Key=SessionReview}"
                                                                  Command="{Binding #root.StartReviewSessionCommandProxy}"
                                                                  CommandParameter="{Binding}" />
                                                        <MenuItem Header="{markup:T Ns=Flashcards, Key=SessionQuick}"
                                                                  Command="{Binding #root.StartQuickSessionCommandProxy}"
                                                                  CommandParameter="{Binding}" />
                                                        <MenuItem Header="{markup:T Ns=Flashcards, Key=SessionCram}"
                                                                  Command="{Binding #root.StartCramSessionCommandProxy}"
                                                                  CommandParameter="{Binding}" />
                                                        <MenuItem Header="{markup:T Ns=Flashcards, Key=SessionTest}"
                                                                  Command="{Binding #root.StartTestSessionCommandProxy}"
                                                                  CommandParameter="{Binding}" />
                                                    </MenuItem>
                                                    <MenuItem Header="{markup:T Ns=Flashcards, Key=RenameDeck}"
                                                              Tag="{Binding}"
                                                              Click="OnDeckRenameClick">
                                                        <MenuItem.Icon>
                                                            <controls:SvgIcon SvgPath="avares://Mnemo.UI/Icons/Flyout/rename.svg"
                                                                              Width="16" Height="16" />
                                                        </MenuItem.Icon>
                                                    </MenuItem>
                                                    <MenuItem Header="{markup:T Ns=Flashcards, Key=DuplicateDeck}"
                                                              Tag="{Binding}"
                                                              Click="OnDeckDuplicateClick">
                                                        <MenuItem.Icon>
                                                            <controls:SvgIcon SvgPath="avares://Mnemo.UI/Icons/Flyout/duplicate.svg"
                                                                              Width="16" Height="16" />
                                                        </MenuItem.Icon>
                                                    </MenuItem>
                                                    <MenuItem Header="{markup:T Ns=Flashcards, Key=Export}" Tag="{Binding}" Click="OnDeckExportClick">
                                                        <MenuItem.Icon>
                                                            <controls:SvgIcon SvgPath="avares://Mnemo.UI/Icons/Flyout/export.svg"
                                                                              Width="16" Height="16" />
                                                        </MenuItem.Icon>
                                                        <MenuItem Header="{markup:T Ns=Flashcards, Key=ExportFormatMnemo}"
                                                                  Tag="{Binding}"
                                                                  CommandParameter="flashcards.mnemo"
                                                                  Click="OnDeckExportClick" />
                                                        <MenuItem Header="{markup:T Ns=Flashcards, Key=ExportFormatAnki}"
                                                                  Tag="{Binding}"
                                                                  CommandParameter="flashcards.anki"
                                                                  Click="OnDeckExportClick" />
                                                        <MenuItem Header="{markup:T Ns=Flashcards, Key=ExportFormatCsv}"
                                                                  Tag="{Binding}"
                                                                  CommandParameter="flashcards.csv"
                                                                  Click="OnDeckExportClick" />
                                                        <MenuItem Header="{markup:T Ns=Flashcards, Key=ChooseFormat}"
                                                                  Tag="{Binding}"
                                                                  Click="OnDeckExportClick" />
                                                    </MenuItem>
                                                    <MenuItem Header="{markup:T Ns=Settings, Key=Title}"
                                                              Command="{Binding #root.OpenDeckSettingsCommandProxy}"
                                                              CommandParameter="{Binding}">
                                                        <MenuItem.Icon>
                                                            <controls:SvgIcon SvgPath="avares://Mnemo.UI/Icons/Flyout/settings.svg"
                                                                              Width="16" Height="16" />
                                                        </MenuItem.Icon>
                                                    </MenuItem>
                                                    <Separator />
                                                    <MenuItem Classes="danger"
                                                              Header="{markup:T Ns=Flashcards, Key=DeleteDeck}"
                                                              Command="{Binding #root.DeleteDeckCommandProxy}"
                                                              CommandParameter="{Binding}">
                                                        <MenuItem.Icon>
                                                            <controls:SvgIcon SvgPath="avares://Mnemo.UI/Icons/Common/trash.svg"
                                                                              Width="16" Height="16" />
                                                        </MenuItem.Icon>
                                                    </MenuItem>
                                                </MenuFlyout>
                                            </Button.Flyout>
                                        </Button>
                                    </Grid>

                                    <StackPanel Grid.Row="1" VerticalAlignment="Center" Spacing="8" Margin="0,12,0,12">
                                        <Grid ColumnDefinitions="*,Auto">
                                            <TextBlock Grid.Column="0"
                                                       Text="{markup:T Ns=Flashcards, Key=DeckRetentionLabel}"
                                                       FontSize="{DynamicResource FontSize.Body.Small}"
                                                       Foreground="{DynamicResource TextSecondaryBrush}" />
                                            <TextBlock Grid.Column="1"
                                                       Text="{Binding RetentionScore, StringFormat={}{0}%}"
                                                       FontSize="{DynamicResource FontSize.Body.Small}"
                                                       Foreground="{DynamicResource TextSecondaryBrush}" />
                                        </Grid>
                                        <Button Background="Transparent"
                                                BorderThickness="0"
                                                Padding="0"
                                                HorizontalAlignment="Stretch"
                                                Command="{Binding #root.StartQuickSessionCommandProxy}"
                                                CommandParameter="{Binding}"
                                                ToolTip.Tip="{markup:T Ns=Flashcards, Key=SessionQuickStart}">
                                            <ProgressBar Minimum="0" Maximum="100"
                                                         Value="{Binding RetentionPercent}"
                                                         Height="6" CornerRadius="3"
                                                         Foreground="{DynamicResource CardAccentBrush}" />
                                        </Button>
                                    </StackPanel>

                                    <Grid Grid.Row="2" ColumnDefinitions="*,Auto">
                                        <TextBlock Grid.Column="0" Text="{Binding CardCountLine}"
                                                   FontSize="{DynamicResource FontSize.Body.Small}"
                                                   Foreground="{DynamicResource TextSecondaryBrush}" />
                                        <TextBlock Grid.Column="1" Text="{Binding LastStudiedLine}"
                                                   FontSize="{DynamicResource FontSize.Body.Small}"
                                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                                   TextAlignment="Right" />
                                    </Grid>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                </StackPanel>
            </ScrollViewer>
        </SplitView>

        <Canvas x:Name="DragOverlayCanvas" IsHitTestVisible="False" ZIndex="999" />
    </Panel>
</UserControl>
