/** The project's home on GitHub. Every link into it is a path under this one. */
export const REPOSITORY_URL = "https://github.com/onemnemo/mnemo"

/** The downloads, for a build that cannot update itself. */
export const RELEASES_URL = `${REPOSITORY_URL}/releases`

/** The issue form, where a bug report goes. */
export const NEW_ISSUE_URL = `${REPOSITORY_URL}/issues/new`
