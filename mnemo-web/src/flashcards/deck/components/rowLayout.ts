// The card table's column track, shared by the header strip and every row so the
// two stay locked together: checkbox, front, type, tags, due, lapses.
export const ROW_GRID = "grid grid-cols-[28px_minmax(0,1fr)_90px_140px_80px_64px] items-center px-[14px]"

export const MONO_CELL = "text-right font-mono text-caption tabular-nums"
